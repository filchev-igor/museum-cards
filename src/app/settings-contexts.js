import React from "react";

const AuthorContext = React.createContext(null);

const DateContext = React.createContext(null);

const HideContext = React.createContext(null);

const CardRefContext = React.createContext(null);

const DefaultImageLinkContext = React.createContext(null);

export {AuthorContext, DateContext, HideContext, CardRefContext, DefaultImageLinkContext};